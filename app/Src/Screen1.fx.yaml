Screen1 As screen:

    Button4 As button:
        OnSelect: |
            =Set(varAttachment, JSON(Image1.Image, JSONFormat.IncludeBinaryData));
            Set(varBase64Only, Mid(varAttachment, Find(",", varAttachment)+1, Len(varAttachment)-Find(",", varAttachment) - 1));
        Text: ="Button"
        X: =1067
        Y: =94
        ZIndex: =2

    AttachmentControl As attachments:
        BorderColor: =
        Height: =120
        IsInDataCard: =true
        Items: =
        OnAddFile: =
        PaddingBottom: =5
        PaddingLeft: =If(Self.DisplayMode = DisplayMode.Edit, 5, 0)
        PaddingRight: =5
        PaddingTop: =5
        Tooltip: =
        Width: =(Parent.Width - 60) * 0.6
        X: =231
        Y: =54
        ZIndex: =3

    Image1 As image:
        Height: =122
        Image: =Last(AttachmentControl.Attachments).Value
        Width: =770
        X: =231
        Y: =222
        ZIndex: =4

    Label3 As label:
        Height: =159
        Text: =varAttachment
        Width: =757
        X: =231
        Y: =455
        ZIndex: =5

