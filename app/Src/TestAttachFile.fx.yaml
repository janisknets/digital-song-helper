TestAttachFile As screen:

    Button4 As button:
        OnSelect: |-
            =Set(varAttachmentTest, JSON(Image1.Image, JSONFormat.IncludeBinaryData));
            Set(varBase64Only, Mid(varAttachmentTest, Find(",", varAttachmentTest)+1, Len(varAttachmentTest)-Find(",", varAttachmentTest) - 1));
            UploadFileTest.Run(Last(AttachmentControl.Attachments).Name, varBase64Only)
        Text: ="Button"
        X: =1065
        Y: =155
        ZIndex: =2

    AttachmentControl As attachments:
        BorderColor: =
        Height: =120
        IsInDataCard: =true
        Items: =
        OnAddFile: =
        PaddingBottom: =5
        PaddingLeft: =If(Self.DisplayMode = DisplayMode.Edit, 5, 0)
        PaddingRight: =5
        PaddingTop: =5
        Tooltip: =
        Width: =(Parent.Width - 60) * 0.6
        X: =218
        Y: =134
        ZIndex: =3

    Image1 As image:
        Height: =122
        Image: =Last(AttachmentControl.Attachments).Value
        Width: =770
        X: =218
        Y: =264
        ZIndex: =4

