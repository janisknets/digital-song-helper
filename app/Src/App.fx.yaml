App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =Set(varMenuItems, Table(
            {Title: "Campaings List", Screen: CampaignScreen}, 
            {Title: "Templates List", Screen: TemplatesScreen}
        ));
        Set(varCurrentUser, User());
        TemplatesScreen
    StartScreen: =CampaignScreen

